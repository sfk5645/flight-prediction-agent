# Runtime deps for Streamlit Community Cloud (UI + agent + predict + R2 lake tools).
# Full project (dbt / MLflow / FastAPI) is not required on the Streamlit host —
# GitHub Actions handles ingest/train and publishes the model to R2.

streamlit>=1.40.0,<1.60.0
boto3>=1.35.0
duckdb>=1.1.0
httpx>=0.27.0
joblib>=1.4.0
langchain-core>=0.3.0
langchain-groq>=0.2.0
langgraph>=0.2.0
numpy>=1.26.0
pandas>=2.2.0
pyarrow>=17.0.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
scikit-learn>=1.5.0
xgboost>=2.1.0
